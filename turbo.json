{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local", "**/.env", ".env"],
  "tasks": {
    "type-check": {
      "cache": false,
      "persistent": true
    }
  }
}
